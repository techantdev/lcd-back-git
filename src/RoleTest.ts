/*

import { Table } from '@typedorm/common'; // npm i @typedorm/common

// CREACION DE LA TABLA GLOBAL
const myTable = new Table({
  name: 'my-table',
  partitionKey: 'PK',
  sortKey: 'SK',
  indexes: {
    GSI1: {
      type: INDEX_TYPE.GSI,
      partitionKey: 'GSI1PK',
      sortKey: 'GSI1SK'
    },
    GSI2: {
      type: INDEX_TYPE.GSI,
      partitionKey: 'GSI2PK',
      sortKey: 'GSI2SK'
    },
    GSI3: {
      type: INDEX_TYPE.GSI,
      partitionKey: 'GSI3PK',
      sortKey: 'GSI3SK'
    }
  }
});

export { myTable };
*/
